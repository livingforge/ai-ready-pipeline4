# Workflow transport references

<!-- Generated by cargo run --locked --example sync_docs. Do not edit. -->

正本: `contracts/agent-reference-schema.json`。更新は `cargo run --locked --example sync_docs`。

各行は JSON Pointer とその位置の制約です。子スキーマは後続行へ展開しています。構造以外の検証・判断基準は利用ガイドを参照してください。

## Reference table

| JSON Pointer | 制約 |
| --- | --- |
| <code></code> | <code>{"$schema":"https://json-schema.org/draft/2020-12/schema","description":"Transport references consist of a run UUID encoded in base 36 and a monotonic sequence number. The persisted table maps each reference to a full SHA-256 digest; entries are never reassigned or removed during a run.","propertyNames":{"$ref":"#/$defs/reference"},"type":"object"}</code> |
| <code>/$defs/namespace</code> | <code>{"pattern":"^[0-9a-z]{25}$","type":"string"}</code> |
| <code>/$defs/reference</code> | <code>{"pattern":"^r[0-9a-z]{25}\\.[1-9][0-9]*$","type":"string"}</code> |
| <code>/additionalProperties</code> | <code>{"pattern":"^[a-f0-9]{64}$","type":"string"}</code> |
