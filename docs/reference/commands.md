# CLI コマンド・引数一覧

<!-- Generated by cargo run --locked --example sync_docs. Do not edit. -->

正本: `crates/arp4-cli/src/cli.rs` と各コマンドの clap 定義。更新は `cargo run --locked --example sync_docs`。

## `arp4`

```text
ARP Rust: Excel, Word, PPTX and PDF document workflow

Usage: arp4 [OPTIONS] <COMMAND>

Commands:
  spec       Build and validate evidence-backed specification models
  doctor
  skills
  documents
  help       Print this message or the help of the given subcommand(s)

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help

  -V, --version
          Print version
```

## `arp4 spec`

```text
Build and validate evidence-backed specification models

Usage: arp4 spec [OPTIONS] <COMMAND>

Commands:
  structure   Interpret original Excel structure without changing workbook contents or formatting
  workflow    Resume evidence-backed generation with pluggable model runners
  semantic    Prepare small semantic tasks and expand source-bound replies deterministically
  registry    Maintain canonical requirement/specification revisions and derived views
  assign-ids  Assign persistent sequential IDs using an explicit identity plan
  sources     Write complete source text in document/sheet/cell order
  schema      Save the full JSON Schema for extraction output
  capture     Capture parser extraction JSON files as a fixed input bundle
  prompt      Write the extraction and independent review instructions
  check
  render
  help        Print this message or the help of the given subcommand(s)

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec structure`

```text
Interpret original Excel structure without changing workbook contents or formatting

Usage: arp4 spec structure [OPTIONS] <COMMAND>

Commands:
  render  Render an element or visual's Excel range to PNG and register it as evidence. Requires Windows and desktop Excel; does not save changes to the workbook
  init    Create a read-only interpretation YAML from Excel, DOCX, PPTX or PDF extraction
  check   Validate source/image versions, cell references and review freshness
  read    Read the interpretation, original cells, compact drawings and verified image paths
  region  Register an existing PNG region on an element or visual; does not render Excel
  review  Record an actual review of this exact interpretation, including image hashes
  schema  Save the authoritative interpretation schema
  help    Print this message or the help of the given subcommand(s)

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>
          Repository root used to verify original and image paths

          [default: .]

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec structure render`

```text
Render an element or visual's Excel range to PNG and register it as evidence. Requires Windows and desktop Excel; does not save changes to the workbook

Usage: arp4 spec structure render [OPTIONS] --extraction <EXTRACTION> --structure <STRUCTURE> --element <ELEMENT> --id <ID> --image <IMAGE>

Options:
      --extraction <EXTRACTION>


      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>
          Repository root used to verify original and image paths

          [default: .]

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --structure <STRUCTURE>


      --element <ELEMENT>


      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --id <ID>


      --image <IMAGE>
          New repository-relative PNG path outside disposable cache/work

      --range <RANGE>
          A1 range; omitted uses the visual's cell anchors, or the element sheet's UsedRange

      --timeout <TIMEOUT>
          [default: 120]

  -h, --help
          Print help
```

## `arp4 spec structure init`

```text
Create a read-only interpretation YAML from Excel, DOCX, PPTX or PDF extraction

Usage: arp4 spec structure init [OPTIONS] --extraction <EXTRACTION> --out <OUT>

Options:
      --extraction <EXTRACTION>


      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>
          Repository root used to verify original and image paths

          [default: .]

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --out <OUT>


      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec structure check`

```text
Validate source/image versions, cell references and review freshness

Usage: arp4 spec structure check [OPTIONS] --extraction <EXTRACTION> --structure <STRUCTURE>

Options:
      --extraction <EXTRACTION>


      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>
          Repository root used to verify original and image paths

          [default: .]

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --structure <STRUCTURE>


      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec structure read`

```text
Read the interpretation, original cells, compact drawings and verified image paths

Usage: arp4 spec structure read [OPTIONS] --extraction <EXTRACTION> --structure <STRUCTURE>

Options:
      --extraction <EXTRACTION>


      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>
          Repository root used to verify original and image paths

          [default: .]

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --structure <STRUCTURE>


      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec structure region`

```text
Register an existing PNG region on an element or visual; does not render Excel

Usage: arp4 spec structure region [OPTIONS] --extraction <EXTRACTION> --structure <STRUCTURE> --element <ELEMENT> --id <ID> --image <IMAGE> --range <RANGE>

Options:
      --extraction <EXTRACTION>


      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>
          Repository root used to verify original and image paths

          [default: .]

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --structure <STRUCTURE>


      --element <ELEMENT>


      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --id <ID>


      --image <IMAGE>
          Repository-relative PNG path; keep evidence outside disposable work/cache directories

      --range <RANGE>


      --bbox <BBOX> <BBOX> <BBOX> <BBOX>
          Normalized x,y,width,height in the supplied full image

          [default: 0,0,1,1]

  -h, --help
          Print help
```

## `arp4 spec structure review`

```text
Record an actual review of this exact interpretation, including image hashes

Usage: arp4 spec structure review [OPTIONS] --extraction <EXTRACTION> --structure <STRUCTURE> --decision <DECISION> --actor <ACTOR> --reason <REASON>

Options:
      --extraction <EXTRACTION>


      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>
          Repository root used to verify original and image paths

          [default: .]

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --structure <STRUCTURE>


      --decision <DECISION>
          [possible values: accepted, rejected]

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --actor <ACTOR>


      --reason <REASON>


  -h, --help
          Print help
```

## `arp4 spec structure schema`

```text
Save the authoritative interpretation schema

Usage: arp4 spec structure schema [OPTIONS] --out <OUT>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --out <OUT>


      --root <ROOT>
          Repository root used to verify original and image paths

          [default: .]

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec workflow`

```text
Resume evidence-backed generation with pluggable model runners

Usage: arp4 spec workflow [OPTIONS] <COMMAND>

Commands:
  init
  status
  next            Read the next pending task without advancing state or invoking a model
  handoff         Prepare a compact worker assignment without reading task evidence or claiming it
  receipt         Confirm the latest recorded submission, including tasks retired after advance
  read            Read the complete task, automatically traversing nested data in bounded pages. Start without --pointer. Continue with page.next_command for your shell, keeping the same executable and workflow root/run-id prefix until page.complete
  validate-reply  Check a complete reply or draft correction without saving artifacts or history
  history         Inspect provider calls and external intervention history
  record-usage    Attach measured deltas to one external submission, including a retired task. Existing measurements may be repeated identically, but never overwritten or added twice
  advance
  inspect
  submit          Submit a complete reply or a revision-bound draft correction; validate the whole result
  update
  run
  recover
  retry
  export
  compact
  help            Print this message or the help of the given subcommand(s)

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>
          Repository root; otherwise discover .arp/config.yml within the Git boundary

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --run-id <RUN_ID>
          Local execution to resume (not a document version)

          [default: current]

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec workflow init`

```text
Usage: arp4 spec workflow init [OPTIONS] --input <INPUT>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --review-plan <REVIEW_PLAN>
          JSON review plan with source IDs omitted from review and explicit reasons

      --input <INPUT>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --reply <REPLY>...


      --regions <REGIONS>


      --max-rounds <MAX_ROUNDS>
          Maximum repair/restructure waves for the entire run; then confirm and emit a draft if issues remain

          [default: 2]

      --review-granularity <REVIEW_GRANULARITY>
          Review unit selection. Adaptive batches small documents and splits large ones by sheet

          [default: adaptive]
          [possible values: adaptive, document, sheet]

      --modules <MODULES>
          Shared module slug/name vocabulary (JSON object)

      --extract-max-chars <EXTRACT_MAX_CHARS>
          Optional source-text character cap; zero disables it

          [default: 0]

      --extract-max-sources <EXTRACT_MAX_SOURCES>
          Optional source count cap; zero disables it

          [default: 0]

      --extract-max-bytes <EXTRACT_MAX_BYTES>
          UTF-8 task size, including instructions, schema and context

          [default: 98304]

      --reference <REFERENCE>...
          Reference text files (Markdown, plain text): supporting context for review and repair, readable with read --pointer references. Never a source

  -h, --help
          Print help
```

## `arp4 spec workflow status`

```text
Usage: arp4 spec workflow status [OPTIONS]

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --summary
          Omit detailed diagnostics and accounting from the orchestration view

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec workflow next`

```text
Read the next pending task without advancing state or invoking a model

Usage: arp4 spec workflow next [OPTIONS]

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec workflow handoff`

```text
Prepare a compact worker assignment without reading task evidence or claiming it

Usage: arp4 spec workflow handoff [OPTIONS] --task <TASK>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --task <TASK>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec workflow receipt`

```text
Confirm the latest recorded submission, including tasks retired after advance

Usage: arp4 spec workflow receipt [OPTIONS] --task <TASK>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --task <TASK>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec workflow read`

```text
Read the complete task, automatically traversing nested data in bounded pages. Start without --pointer. Continue with page.next_command for your shell, keeping the same executable and workflow root/run-id prefix until page.complete

Usage: arp4 spec workflow read [OPTIONS] --task <TASK>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --task <TASK>


      --pointer <POINTER>
          [default: ""]

      --offset <OFFSET>
          [default: 0]

      --limit <LIMIT>
          Optional fragment count cap

          [default: 10000]

      --max-bytes <MAX_BYTES>
          Maximum reading response bytes (configured TOON or JSON). Lower for host truncation

          [default: 48000]

      --revision <REVISION>
          Copy page.revision when continuing; changed content requires restarting at offset 0

  -h, --help
          Print help
```

## `arp4 spec workflow validate-reply`

```text
Check a complete reply or draft correction without saving artifacts or history

Usage: arp4 spec workflow validate-reply [OPTIONS] --task <TASK>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --task <TASK>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --reply <REPLY>


      --json <JSON>
          Inline JSON; use --reply - for large UTF-8 replies

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec workflow history`

```text
Inspect provider calls and external intervention history

Usage: arp4 spec workflow history [OPTIONS]

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec workflow record-usage`

```text
Attach measured deltas to one external submission, including a retired task. Existing measurements may be repeated identically, but never overwritten or added twice

Usage: arp4 spec workflow record-usage [OPTIONS] --task <TASK> --submission <SUBMISSION> --usage-file <USAGE_FILE>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --task <TASK>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --submission <SUBMISSION>
          One-based submission number within the task, as shown by history/receipt

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --usage-file <USAGE_FILE>
          Same measured usage JSON as submit --usage-file; no agent lifetime totals

  -h, --help
          Print help
```

## `arp4 spec workflow advance`

```text
Usage: arp4 spec workflow advance [OPTIONS]

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec workflow inspect`

```text
Usage: arp4 spec workflow inspect [OPTIONS] --task <TASK>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --task <TASK>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec workflow submit`

```text
Submit a complete reply or a revision-bound draft correction; validate the whole result

Usage: arp4 spec workflow submit [OPTIONS] --task <TASK>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --task <TASK>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --reply <REPLY>


      --json <JSON>


      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --actor <ACTOR>
          [default: external]

      --model <MODEL>


      --reason <REASON>
          Short inline reason; defaults to "Externally supplied reply" when omitted

      --reason-file <REASON_FILE>
          Read the reason verbatim from a UTF-8 text file (optional BOM)

      --origin <ORIGIN>
          [default: external]
          [possible values: external, interactive-agent]

      --usage-file <USAGE_FILE>
          JSON with measured usage and reported_cost_usd; never estimates

  -h, --help
          Print help
```

## `arp4 spec workflow update`

```text
Usage: arp4 spec workflow update [OPTIONS]

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --input <INPUT>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --reply <REPLY>...


      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --regions <REGIONS>


      --reference <REFERENCE>...
          Replace the reference text files of the run

  -h, --help
          Print help
```

## `arp4 spec workflow run`

```text
Usage: arp4 spec workflow run [OPTIONS] --provider <PROVIDER> --executable <EXECUTABLE> --model <MODEL>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --provider <PROVIDER>
          [possible values: claude-code, command]

      --executable <EXECUTABLE>
          Absolute or relative path to the provider executable; no shell evaluation

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --arg <ARGUMENTS>
          Arguments for the command provider, repeated as --arg=value

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --model <MODEL>


      --max-calls <MAX_CALLS>
          [default: 1]

      --max-budget-usd <MAX_BUDGET_USD>
          Claude Code only; per-call budget, default 2 USD. Rejected by command provider

      --timeout <TIMEOUT>
          [default: 900]

      --extract-max-tokens <EXTRACT_MAX_TOKENS>
          [default: 16000]

      --repair-max-tokens <REPAIR_MAX_TOKENS>
          [default: 8000]

      --review-max-tokens <REVIEW_MAX_TOKENS>
          [default: 12000]

      --extract-effort <EXTRACT_EFFORT>
          [default: medium]
          [possible values: low, medium, high]

      --repair-effort <REPAIR_EFFORT>
          [default: adaptive]
          [possible values: adaptive, low, medium, high]

      --review-effort <REVIEW_EFFORT>
          [default: high]
          [possible values: low, medium, high]

      --total-budget-usd <TOTAL_BUDGET_USD>
          Total reported cost ceiling for this workflow (Claude only, including previous calls)

      --max-retries <MAX_RETRIES>
          Opt-in bounded recovery for transient failures or invalid replies, within max-calls

          [default: 0]

  -h, --help
          Print help
```

## `arp4 spec workflow recover`

```text
Usage: arp4 spec workflow recover [OPTIONS] --task <TASK>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --task <TASK>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec workflow retry`

```text
Usage: arp4 spec workflow retry [OPTIONS] --task <TASK>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --task <TASK>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --reason <REASON>
          Short inline reason; use --reason-file for quotes or multiline text

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --reason-file <REASON_FILE>
          Read the reason verbatim from a UTF-8 text file (optional BOM)

  -h, --help
          Print help
```

## `arp4 spec workflow export`

```text
Usage: arp4 spec workflow export [OPTIONS]

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec workflow compact`

```text
Usage: arp4 spec workflow compact [OPTIONS]

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec semantic`

```text
Prepare small semantic tasks and expand source-bound replies deterministically

Usage: arp4 spec semantic [OPTIONS] <COMMAND>

Commands:
  changes        Plan document-level re-extraction; changed claims always require global review
  packet
  assemble
  review-packet
  review-apply
  finalize       Assign initial persistent IDs and remap the classification catalog. Not an update command
  help           Print this message or the help of the given subcommand(s)

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>
          Project configuration for agent reading material; otherwise use the current repository

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec semantic changes`

```text
Plan document-level re-extraction; changed claims always require global review

Usage: arp4 spec semantic changes [OPTIONS] --before <BEFORE> --after <AFTER> --out <OUT>

Options:
      --before <BEFORE>


      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>
          Project configuration for agent reading material; otherwise use the current repository

      --after <AFTER>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --out <OUT>


  -h, --help
          Print help
```

## `arp4 spec semantic packet`

```text
Usage: arp4 spec semantic packet [OPTIONS] --input <INPUT> --document <DOCUMENT> --out <OUT>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --input <INPUT>


      --root <ROOT>
          Project configuration for agent reading material; otherwise use the current repository

      --document <DOCUMENT>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --out <OUT>


  -h, --help
          Print help
```

## `arp4 spec semantic assemble`

```text
Usage: arp4 spec semantic assemble [OPTIONS] --input <INPUT> --reply <REPLY>... --actor <ACTOR> --out <OUT>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --input <INPUT>


      --root <ROOT>
          Project configuration for agent reading material; otherwise use the current repository

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --reply <REPLY>...


      --actor <ACTOR>


      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --out <OUT>


  -h, --help
          Print help
```

## `arp4 spec semantic review-packet`

```text
Usage: arp4 spec semantic review-packet [OPTIONS] --input <INPUT> --model <MODEL> --catalog <CATALOG> --out <OUT>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>
          Project configuration for agent reading material; otherwise use the current repository

      --source <SOURCE>...
          Explicit audit sources; common headings/notes belong in --context-source

      --context-source <CONTEXT_SOURCE>...


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --sheet <SHEET>
          Review all source text on one sheet, retaining document source aliases

      --expanded
          Emit expanded logical data for inspection/measurement instead of lossless tables

      --input <INPUT>


      --model <MODEL>


      --catalog <CATALOG>


      --document <DOCUMENT>


      --out <OUT>


  -h, --help
          Print help
```

## `arp4 spec semantic review-apply`

```text
Usage: arp4 spec semantic review-apply [OPTIONS] --input <INPUT> --model <MODEL> --catalog <CATALOG> --review <REVIEW>... --reviewer <REVIEWER> --out <OUT>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --review-plan <REVIEW_PLAN>
          Explicit source IDs omitted from independent review, with reasons

      --root <ROOT>
          Project configuration for agent reading material; otherwise use the current repository

      --input <INPUT>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --model <MODEL>


      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --catalog <CATALOG>


      --review <REVIEW>...


      --reviewer <REVIEWER>


      --out <OUT>


  -h, --help
          Print help
```

## `arp4 spec semantic finalize`

```text
Assign initial persistent IDs and remap the classification catalog. Not an update command

Usage: arp4 spec semantic finalize [OPTIONS] --input <INPUT> --model <MODEL> --catalog <CATALOG> --plan <PLAN> --out <OUT>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --input <INPUT>


      --root <ROOT>
          Project configuration for agent reading material; otherwise use the current repository

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --model <MODEL>


      --catalog <CATALOG>


      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --plan <PLAN>


      --out <OUT>


      --registry
          Also save the validated initial canonical registry revision

      --project <PROJECT>
          Project name for the initial canonical registry

  -h, --help
          Print help
```

## `arp4 spec registry`

```text
Maintain canonical requirement/specification revisions and derived views

Usage: arp4 spec registry [OPTIONS] <COMMAND>

Commands:
  init       Import reviewed extraction with explicit classifications; never auto-approve
  preflight  Check a finalized bundle without creating a registry
  apply      Apply an explicit change against a fingerprinted base to the current registry
  check      Verify integrity, evidence, lifecycle and relationships
  render     Render the current registry into .arp/cache/registry
  help       Print this message or the help of the given subcommand(s)

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>
          Repository root; otherwise discover .arp/config.yml

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec registry init`

```text
Import reviewed extraction with explicit classifications; never auto-approve

Usage: arp4 spec registry init [OPTIONS] --input <INPUT> --model <MODEL> --catalog <CATALOG> --project <PROJECT>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --input <INPUT>


      --root <ROOT>
          Repository root; otherwise discover .arp/config.yml

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --model <MODEL>


      --catalog <CATALOG>


      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --project <PROJECT>


  -h, --help
          Print help
```

## `arp4 spec registry preflight`

```text
Check a finalized bundle without creating a registry

Usage: arp4 spec registry preflight [OPTIONS] --input <INPUT> --model <MODEL> --catalog <CATALOG> --project <PROJECT>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --input <INPUT>


      --root <ROOT>
          Repository root; otherwise discover .arp/config.yml

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --model <MODEL>


      --catalog <CATALOG>


      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --project <PROJECT>


  -h, --help
          Print help
```

## `arp4 spec registry apply`

```text
Apply an explicit change against a fingerprinted base to the current registry

Usage: arp4 spec registry apply [OPTIONS] --change <CHANGE>

Options:
      --change <CHANGE>


      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>
          Repository root; otherwise discover .arp/config.yml

      --input <INPUT>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec registry check`

```text
Verify integrity, evidence, lifecycle and relationships

Usage: arp4 spec registry check [OPTIONS]

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>
          Repository root; otherwise discover .arp/config.yml

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec registry render`

```text
Render the current registry into .arp/cache/registry

Usage: arp4 spec registry render [OPTIONS]

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>
          Repository root; otherwise discover .arp/config.yml

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec assign-ids`

```text
Assign persistent sequential IDs using an explicit identity plan

Usage: arp4 spec assign-ids [OPTIONS] --input <INPUT> --model <MODEL> --plan <PLAN> --out <OUT>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --input <INPUT>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --model <MODEL>


      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --plan <PLAN>


      --previous <PREVIOUS>
          Previous assigned model; its .ids.json ledger is required

      --out <OUT>


  -h, --help
          Print help
```

## `arp4 spec sources`

```text
Write complete source text in document/sheet/cell order

Usage: arp4 spec sources [OPTIONS] --input <INPUT> --out <OUT>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --input <INPUT>


      --document <DOCUMENT>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --out <OUT>


  -h, --help
          Print help
```

## `arp4 spec schema`

```text
Save the full JSON Schema for extraction output

Usage: arp4 spec schema [OPTIONS] --out <OUT>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --out <OUT>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec capture`

```text
Capture parser extraction JSON files as a fixed input bundle

Usage: arp4 spec capture [OPTIONS] --extraction <EXTRACTION>... --out <OUT>

Options:
      --extraction <EXTRACTION>...


      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --document <DOCUMENT>...
          Reviewed documents whose canonical structure corrections should be captured

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --root <ROOT>


      --out <OUT>


  -h, --help
          Print help
```

## `arp4 spec prompt`

```text
Write the extraction and independent review instructions

Usage: arp4 spec prompt [OPTIONS] --out <OUT>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --out <OUT>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 spec check`

```text
Usage: arp4 spec check [OPTIONS] --input <INPUT> --model <MODEL>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --input <INPUT>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --model <MODEL>


      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --out <OUT>
          Save all diagnostics to a file instead of a page on stdout

  -h, --help
          Print help
```

## `arp4 spec render`

```text
Usage: arp4 spec render [OPTIONS] --input <INPUT> --model <MODEL> --out <OUT>

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --input <INPUT>


      --limit <LIMIT>
          Maximum items per page (default: 20)

      --model <MODEL>


      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --out <OUT>


      --draft


  -h, --help
          Print help
```

## `arp4 doctor`

```text
Usage: arp4 doctor [OPTIONS]

Options:
      --format <FORMAT>
          [default: json]
          [possible values: json]

      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 skills`

```text
Usage: arp4 skills [OPTIONS] <COMMAND>

Commands:
  install  Install ARP skills, references and custom agents for the selected host
  help     Print this message or the help of the given subcommand(s)

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 skills install`

```text
Install ARP skills, references and custom agents for the selected host

Usage: arp4 skills install [OPTIONS]

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>
          [default: .]

      --agent <AGENT>
          [default: all]
          [possible values: all, claude, github, codex, none]

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 documents`

```text
Usage: arp4 documents [OPTIONS] <COMMAND>

Commands:
  schema
  init
  import
  structure-read  Write a derived structure view from the document's canonical corrections
  structure-save  Save structure corrections; remove a managed work/structure view on success
  record
  adopt
  review
  diff
  apply           Apply reviewed edits to the original and re-extract for verification
  export
  check
  status
  help            Print this message or the help of the given subcommand(s)

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>


      --include-hashes
          Include verification hashes in record/review/check/status/export output

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 documents schema`

```text
Usage: arp4 documents schema [OPTIONS] <KIND>

Arguments:
  <KIND>


Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --pointer <POINTER>
          Select a schema node using a JSON Pointer, e.g. /properties/entries/items

      --root <ROOT>


      --include-hashes
          Include verification hashes in record/review/check/status/export output

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 documents init`

```text
Usage: arp4 documents init [OPTIONS]

Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>


      --sources <SOURCES>
          [default: docs]

      --include-hashes
          Include verification hashes in record/review/check/status/export output

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 documents import`

```text
Usage: arp4 documents import [OPTIONS] --id <ID> <SOURCE>

Arguments:
  <SOURCE>


Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --id <ID>
          ASCII ID: letters, digits, underscore or hyphen; start with a letter/digit

      --root <ROOT>


      --include-hashes
          Include verification hashes in record/review/check/status/export output

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 documents structure-read`

```text
Write a derived structure view from the document's canonical corrections

Usage: arp4 documents structure-read [OPTIONS] --out <OUT> <DOCUMENT>

Arguments:
  <DOCUMENT>


Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --out <OUT>


      --root <ROOT>


      --include-hashes
          Include verification hashes in record/review/check/status/export output

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 documents structure-save`

```text
Save structure corrections; remove a managed work/structure view on success

Usage: arp4 documents structure-save [OPTIONS] --input <INPUT> <DOCUMENT>

Arguments:
  <DOCUMENT>


Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --input <INPUT>


      --root <ROOT>


      --include-hashes
          Include verification hashes in record/review/check/status/export output

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 documents record`

```text
Usage: arp4 documents record [OPTIONS] --model <MODEL> --actor <ACTOR> --prompt <PROMPT> <PROPOSAL>

Arguments:
  <PROPOSAL>


Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --model <MODEL>


      --root <ROOT>


      --actor <ACTOR>


      --include-hashes
          Include verification hashes in record/review/check/status/export output

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --prompt <PROMPT>


  -h, --help
          Print help
```

## `arp4 documents adopt`

```text
Usage: arp4 documents adopt [OPTIONS] --reviewer <REVIEWER> <PROPOSAL>

Arguments:
  <PROPOSAL>


Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --reviewer <REVIEWER>


      --root <ROOT>


      --include-hashes
          Include verification hashes in record/review/check/status/export output

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 documents review`

```text
Usage: arp4 documents review [OPTIONS] --reviewer <REVIEWER> <DOCUMENT>

Arguments:
  <DOCUMENT>


Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --reviewer <REVIEWER>


      --root <ROOT>


      --include-hashes
          Include verification hashes in record/review/check/status/export output

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 documents diff`

```text
Usage: arp4 documents diff [OPTIONS] [PROPOSAL]

Arguments:
  [PROPOSAL]


Options:
      --document <DOCUMENT>


      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>


      --format <FORMAT>
          [default: json]
          [possible values: json, markdown]

      --include-hashes
          Include verification hashes in record/review/check/status/export output

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

      --out <OUT>


  -h, --help
          Print help
```

## `arp4 documents apply`

```text
Apply reviewed edits to the original and re-extract for verification

Usage: arp4 documents apply [OPTIONS] <DOCUMENT>

Arguments:
  <DOCUMENT>


Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --root <ROOT>


      --include-hashes
          Include verification hashes in record/review/check/status/export output

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 documents export`

```text
Usage: arp4 documents export [OPTIONS] <DOCUMENT>

Arguments:
  <DOCUMENT>


Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --out <OUT>


      --root <ROOT>


      --engine <ENGINE>
          [default: auto]

      --include-hashes
          Include verification hashes in record/review/check/status/export output

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 documents check`

```text
Usage: arp4 documents check [OPTIONS] [DOCUMENT]

Arguments:
  [DOCUMENT]


Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --proposal <PROPOSAL>


      --root <ROOT>


      --include-hashes
          Include verification hashes in record/review/check/status/export output

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --require-reviewed


      --format <FORMAT>
          [default: json]
          [possible values: json]

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```

## `arp4 documents status`

```text
Usage: arp4 documents status [OPTIONS] [DOCUMENT]

Arguments:
  [DOCUMENT]


Options:
      --full
          Return all details (not supported by spec workflow; use read pagination there)

      --proposal <PROPOSAL>


      --root <ROOT>


      --format <FORMAT>
          [default: json]
          [possible values: json]

      --include-hashes
          Include verification hashes in record/review/check/status/export output

      --limit <LIMIT>
          Maximum items per page (default: 20)

      --offset <OFFSET>
          Zero-based item offset (default: 0); use page.next_offset for the next page

  -h, --help
          Print help
```
