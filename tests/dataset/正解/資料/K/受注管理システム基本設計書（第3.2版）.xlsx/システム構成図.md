# 受注管理システム基本設計書（第3.2版）.xlsx / システム構成図

<!-- source: 資料/K/受注管理システム基本設計書（第3.2版）.xlsx / シート: システム構成図 -->

> このシートには 図形 18 個・接続子 12 本・画像 1 枚 があり、15 個からテキスト（`s4-g1`）、接続 10 本（`s4-c1`）を取り出しました。ただし接続子 1 本は、繋がってはいますが相手の図形が文字を持ちません（ゾーンの囲み枠・装飾）。どこへ向かう線かは `arp4 render` で絵にすれば読めます。接続子 1 本はどこにも繋がっていません（線を目分量で置いた図）。両端の id が資料に無いので、どこからどこへの線かは絵にしても決まりません。貼り付け画像 1 枚は絵のままです（表のスクリーンショットなら、セルの値としては 1 つも取れていません）。`arp4 render` で撮り直しても中身は変わりません。図形と違い、画像は絵にする以前から絵です。実体は `s4-i1` に出してあります（`images/` の中）。開いて読むのは整理層の仕事です。読み取った内容を整理結果へ書くときは、出典に `s4-i1` を指してください。Windows OCR では文字を 1 つも読めませんでした（`s4-o1`）。うち 1 枚には代替テキストがあるので、何の画像かは `s4-a1` でも分かります。取れていないのは配置です。枠で括られたゾーン・段組み・注記の位置が仕様なら、`arp4 render` で絵にして読み、それでも確定できないときに out_of_scope に kind: 未読取 で宣言してください。

## セル A1:A2  <!-- a:s4-x1 at=A1:A2 -->

- `A1` システム構成図（本番環境）
- `A2` 2026-06-15 時点。DR 環境（大阪）は別紙 5 を参照のこと。

## 表 A41:E47  <!-- a:s4-t1 at=A41:E47 -->

| No | 機器名 | 用途 | 台数 | 保守区分 |
|---|---|---|---|---|
| 1 | FortiGate 200F | 外部 FW | 2 | ベンダ保守（24H） |
| 2 | nginx 1.26 | リバースプロキシ | 2 | 自社運用 |
| 3 | Tomcat 10 / Java 21 | AP サーバ | 2 | 自社運用 |
| 4 | PostgreSQL 16 | DB サーバ（現用／待機） | 2 | ベンダ保守（平日） |
| 5 | Zabbix 7.0 | 監視サーバ | 1 | 自社運用 |
| 6 | NAS（帳票保管） | 帳票 7 年保管 | 1 | ベンダ保守（平日） |

## 代替テキスト（人が書いた説明）  <!-- a:s4-a1 at=代替テキスト 4 件 -->

- `画像` 現行機器ラックの写真（サーバ室で撮影。パッチケーブルは写っているが、どの口とどの口かは読み取れない）
- `図形の群` DMZ セグメント（外部公開。FW とリバースプロキシのみを置く）
- `図形の群` 現用／待機の 2 系統で構成する。DR 環境は別紙 5 を参照
- `図形の群` 運用管理セグメント（バッチと帳票保管。業務セグメントからのみ到達可）

## 画像（ブックから取り出したファイル）  <!-- a:s4-i1 at=画像 1 枚 -->

- `システム構成図-p1.png` 現行機器ラックの写真（サーバ室で撮影。パッチケーブルは写っているが、どの口とどの口かは読み取れない）

![システム構成図-p1.png](../../../../images/資料/K/受注管理システム基本設計書（第3.2版）.xlsx/システム構成図-p1.png)

## 画像の中の文字（Windows OCR が読んだもの。読み違えが混ざります）  <!-- a:s4-o1 at=画像 1 枚 -->

`システム構成図-p1.png`

    文字は見つかりませんでした（図・写真・網点の掛かった絵はこうなります）。開いて見るのは整理層の仕事です。

## 図形（テキスト）  <!-- a:s4-g1 at=図形 18 個・接続子 12 本・画像 1 枚 -->

- `図形1` DMZ セグメント（172.16.10.0/24）
- `図形2` 外部 FW<br>FortiGate 200F
- `図形3` リバースプロキシ<br>nginx 1.26
- `図形4` 業務セグメント（172.16.20.0/24）
- `図形5` AP サーバ #1<br>Tomcat 10 / Java 21
- `図形6` AP サーバ #2<br>Tomcat 10 / Java 21
- `図形7` DB サーバ（現用）<br>PostgreSQL 16
- `図形8` DB サーバ（待機）<br>PostgreSQL 16
- `図形9` 運用管理セグメント（172.16.90.0/24）
- `図形10` バッチサーバ<br>日次 02:00 起動
- `図形11` 帳票 NAS<br>7 年保管（CIFS）
- `図形12` 利用者 PC<br>Web ブラウザ
- `図形13` 取引先 EDI<br>AS2 連携
- `図形14` 監視サーバ<br>Zabbix 7.0
- `図形15` 凡例・注記<br>　1. 実線は同期通信（HTTPS／JDBC）<br>　2. 一点鎖線は非同期のファイル連携<br>　　2-1. 連携先は帳票 NAS（CIFS）<br>　　2-2. 再送は 3 回まで<br>　3. DR 環境は別紙 5 を参照

## 図形の接続  <!-- a:s4-c1 at=接続 10 本 -->

| 元 | 向き | 先 | 名前 | 線種 |
|---|---|---|---|---|
| 利用者 PC<br>Web ブラウザ | → | 外部 FW<br>FortiGate 200F | 図形201 | 実線 |
| 取引先 EDI<br>AS2 連携 | → | リバースプロキシ<br>nginx 1.26 | 図形202 | 実線 |
| 外部 FW<br>FortiGate 200F | → | AP サーバ #1<br>Tomcat 10 / Java 21 | 図形203 | 実線 |
| リバースプロキシ<br>nginx 1.26 | → | AP サーバ #2<br>Tomcat 10 / Java 21 | 図形204 | 実線 |
| AP サーバ #1<br>Tomcat 10 / Java 21 | ― | AP サーバ #2<br>Tomcat 10 / Java 21 | 図形205 | 実線 |
| AP サーバ #1<br>Tomcat 10 / Java 21 | → | DB サーバ（現用）<br>PostgreSQL 16 | 図形206 | 実線 |
| AP サーバ #2<br>Tomcat 10 / Java 21 | → | DB サーバ（待機）<br>PostgreSQL 16 | 図形207 | 実線 |
| DB サーバ（現用）<br>PostgreSQL 16 | → | DB サーバ（待機）<br>PostgreSQL 16 | 図形208 | 実線 |
| バッチサーバ<br>日次 02:00 起動 | → | DB サーバ（現用）<br>PostgreSQL 16 | 図形209 | 実線 |
| バッチサーバ<br>日次 02:00 起動 | → | 帳票 NAS<br>7 年保管（CIFS） | 図形210 | 実線 |
