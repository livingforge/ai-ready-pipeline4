# 新販売管理システム方式提案（第2.1版）.pptx / 5 現行の受注入力画面（ハードコピー）

<!-- source: 資料/O/新販売管理システム方式提案（第2.1版）.pptx / スライド: 5 現行の受注入力画面（ハードコピー） -->

> このスライドには 図形 1 個・画像 1 枚 があり、1 個からテキスト（`s5-g1`）を取り出しました。貼り付け画像 1 枚は絵のままです（表のスクリーンショットなら、セルの値としては 1 つも取れていません）。実体を `images/` に出してあるので、開いて見てください。図形と違い、画像は絵にする以前から絵です。実体は `s5-i1` に出してあります（`images/` の中）。開いて読むのは整理層の仕事です。読み取った内容を整理結果へ書くときは、出典に `s5-i1` を指してください。Windows OCR では文字を 1 つも読めませんでした（`s5-o1`）。うち 1 枚には代替テキストがあるので、何の画像かは `s5-a1` でも分かります。取れていないのは配置です。枠で括られたゾーン・段組み・注記の位置が仕様なら、PowerPoint で開いて読み、それでも確定できないときに out_of_scope に kind: 未読取 で宣言してください。

> スライドのレイアウトとマスターに書かれた文字（ページ番号・フッタ・定型の見出し）は取っていません。文書番号・版・機密区分がフッタにしか書かれていないことがあり、そのときこの写しには 1 文字も出てきません ―― 資料に無いのではありません。要るなら PowerPoint で「表示 → スライドマスター」を開いて確かめてください。

## 発表者ノート（スライドには出てこない）  <!-- a:s5-n1 at=ノート 1 段落 -->

画面の項目は別紙 2 に一覧がある。この絵からは読み取れないので、
移行後の画面設計はそちらを正とすること。

## 代替テキスト（人が書いた説明）  <!-- a:s5-a1 at=代替テキスト 1 件 -->

- `画像` 現行の受注入力画面。項目名は別紙 2 の項目定義書による

## 図形（テキスト）  <!-- a:s5-g1 at=図形 1 個・画像 1 枚 -->

- `図形1` 現行の受注入力画面（ハードコピー）

## 画像（ブックから取り出したファイル）  <!-- a:s5-i1 at=画像 1 枚 -->

- `05_現行の受注入力画面（ハードコピー）-p1.png` 現行の受注入力画面。項目名は別紙 2 の項目定義書による

![05_現行の受注入力画面（ハードコピー）-p1.png](../../../../images/資料/O/新販売管理システム方式提案（第2.1版）.pptx/05_現行の受注入力画面（ハードコピー）-p1.png)

## 画像の中の文字（Windows OCR が読んだもの。読み違えが混ざります）  <!-- a:s5-o1 at=画像 1 枚 -->

`05_現行の受注入力画面（ハードコピー）-p1.png`

    文字は見つかりませんでした（図・写真・網点の掛かった絵はこうなります）。開いて見るのは整理層の仕事です。
