# 新販売管理システム方式提案（第2.1版）.pptx / 11 新システムのゾーン構成（サブゾーン入り）

<!-- source: 資料/O/新販売管理システム方式提案（第2.1版）.pptx / スライド: 11 新システムのゾーン構成（サブゾーン入り） -->

> このスライドには 図形 8 個・接続子 6 本 があり、8 個からテキスト（`s11-g1`）、接続 6 本（`s11-c1`）を取り出しました。取れていないのは配置です。枠で括られたゾーン・段組み・注記の位置が仕様なら、PowerPoint で開いて読み、それでも確定できないときに out_of_scope に kind: 未読取 で宣言してください。

> スライドのレイアウトとマスターに書かれた文字（ページ番号・フッタ・定型の見出し）は取っていません。文書番号・版・機密区分がフッタにしか書かれていないことがあり、そのときこの写しには 1 文字も出てきません ―― 資料に無いのではありません。要るなら PowerPoint で「表示 → スライドマスター」を開いて確かめてください。

## 発表者ノート（スライドには出てこない）  <!-- a:s11-n1 at=ノート 1 段落 -->

日次と月次で実行基盤を分けるのは、月次の締めが日次の遅延に
引きずられないようにするためである（現行はここが 1 つで、
月初の 3 日間は日次が遅れると締めも遅れていた）。

## 代替テキスト（人が書いた説明）  <!-- a:s11-a1 at=代替テキスト 2 件 -->

- `図形の群` 現用／待機の 2 系統。DR サイトへは非同期複製
- `図形の群` 実行順は運用設計書 4.2 による。日次と月次で実行基盤が違う

## 図形（テキスト）  <!-- a:s11-g1 at=図形 8 個・接続子 6 本 -->

- `図形1` 新システムのゾーン構成（サブゾーン入り）
- `図形2` 受注入力<br>（Web）
- `図形3` 与信照会 API
- `図形4` 在庫引当
- `図形5` 売上計上
- `図形6` 在庫締め
- `図形7` 請求締め<br>（月次）
- `図形8` 会計連携

## 図形の接続  <!-- a:s11-c1 at=接続 6 本 -->

| 元 | 向き | 先 | 名前 | 線種 |
|---|---|---|---|---|
| 受注入力<br>（Web） | → | 与信照会 API | 同期 | 実線 |
| 与信照会 API | → | 在庫引当 | 同期 | 実線 |
| 在庫引当 | → | 売上計上 | 夜間バッチ | 破線 |
| 売上計上 | → | 在庫締め | 夜間バッチ | 破線 |
| 在庫締め | → | 請求締め<br>（月次） | 月次 | 一点鎖線 |
| 請求締め<br>（月次） | → | 会計連携 | 月次 | 一点鎖線 |
